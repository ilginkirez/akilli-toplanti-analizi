# meeting_analyzer – Güncellenmiş Gereksinimler
# Oluşturulma: 2026-04
# Modül 1 (VAD) + Modül 2 (OpenVidu Entegrasyonu)

# ─── Temel Ses İşleme ───────────────────────────────────────────────────────
numpy>=1.24.0,<3.0.0          # float32 dizileri, stride tricks
scipy>=1.10.0,<2.0.0          # signal processing, framing

# ─── Ses Dosyası Okuma/Yazma ─────────────────────────────────────────────────
soundfile>=0.12.1,<1.0.0      # WAV okuma/yazma (libsndfile bağımlısı)
pydub>=0.25.1,<1.0.0          # PyDub (FFmpeg yoksa fallback)

# ─── Web Framework & ASGI ───────────────────────────────────────────────────
fastapi>=0.104.0,<1.0.0       # WebSocket/SSE endpoint'leri
uvicorn[standard]>=0.24.0,<1.0.0  # ASGI sunucu

# ─── HTTP & WebSocket Client ────────────────────────────────────────────────
aiohttp>=3.9.0,<4.0.0         # Async HTTP client (OpenVidu REST API)
websockets>=12.0,<14.0        # WebSocket desteği

# ─── Yapılandırma ───────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0   # .env dosyası yükleme

# ─── Test ────────────────────────────────────────────────────────────────────
pytest>=7.4.0,<9.0.0          # Test çerçevesi
pytest-cov>=4.1.0,<6.0.0      # Kod kapsama raporu
pytest-asyncio>=0.23.0,<1.0.0 # Async test desteği
aioresponses>=0.7.6,<1.0.0    # aiohttp mock'lama

# ─── Opsiyonel (Geliştirme Araçları) ─────────────────────────────────────────
# matplotlib>=3.7.0           # VAD sonuçlarını görselleştirmek için
# pyannote.audio>=3.1.0       # Gelecekte derin öğrenme VAD entegrasyonu
