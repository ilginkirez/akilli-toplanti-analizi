
  # Zoom-like screen design

  This is a React frontend application for online meetings.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  